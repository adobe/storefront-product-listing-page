import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { LabelledInput } from "./LabelledInput";

<Meta
    title="UIKit/LabelledInput"
    component={LabelledInput}
    parameters={{
        layout: "centered"
    }}
/>

export const Template = args => <LabelledInput {...args} />;

# LabelledInput



```
import { LabelledInput } from './LabelledInput'
```

<Canvas>
    <Story
        name="Radio"
        args={{
            name: "Homer",
            type: "radio",
            checked: true,
            onChange: e => console.log(e),
            label: "Homer",
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Checkbox"
        args={{
            name: "Bender",
            type: "checkbox",
            checked: false,
            onChange: e => console.log(e),
            label: "Bender",
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
