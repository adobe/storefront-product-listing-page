import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { ScalarFacet } from "../../../components";
import { mockColorFilter } from "../mocks.tsx";

<Meta
    title="PLP widget/ScalarFacet"
    component={ScalarFacet}
    parameters={{
        layout: "centered"
    }}
/>

export const Template = args => <ScalarFacet {...args} />;

# RangeFacet

WIP

```
import { ScalarFacet } from './Facets'
```

<Canvas>
    <Story
        name="ScalarFacet"
        args={{
            filterData: mockColorFilter,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
