import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { RangeFacet } from "../../../components";
import { mockPriceFilter } from "../mocks.tsx";
import { SearchDecorator } from "../../../../.storybook/decorators/SearchDecorator";

<Meta
    title="PLP widget/RangeFacet"
    component={RangeFacet}
    parameters={{
        layout: "centered"
    }}
    decorators={[SearchDecorator]}
/>

export const Template = args => <RangeFacet {...args} />;

# RangeFacet

WIP

```
import { RangeFacet } from './Facets'
```

<Canvas>
    <Story
        name="RangeFacet"
        args={{
            filterData: mockPriceFilter,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
