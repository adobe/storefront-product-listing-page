import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { PreviewHeader } from '../PreviewHeader';
import { mockPriceFilter } from '../mocks.tsx';

<Meta
  title="PLP widget/PreviewHeader"
  component={PreviewHeader}
  parameters={{
    layout: 'centered'
  }}
  decorators={[]}
/>

export const Template = (args) => <PreviewHeader {...args} />;

# PreviewHeader

WIP

```
import { PreviewHeader } from '../PreviewHeader'
```

<Canvas>
  <Story
    name="PreviewHeader"
    args={{
      filterData: mockPriceFilter,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
