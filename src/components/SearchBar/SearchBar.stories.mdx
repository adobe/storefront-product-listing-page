import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { SearchBar } from "../SearchBar";
import { action } from "@storybook/addon-actions"

<Meta
    title="PLP/SearchBar"
    component={SearchBar}
    parameters={{
        layout: "centered"
    }}
/>

export const Template = args => <SearchBar {...args} />;

# SearchBar



```
import { SearchBar } from '../SearchBar'
```

<Canvas>
    <Story
        name="SearchBar"
        args={{ phrase: "", onKeyPress: action('onKeyPress') }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="SearchBar with custom placeholder"
        args={{
            phrase: "",
            onKeyPress: action('onKeyPress'),
            placeholder: "Custom placeholder",
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="SearchBar with phrase"
        args={{
            phrase: "yoga pants",
            onKeyPress: action('onKeyPress'),
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
