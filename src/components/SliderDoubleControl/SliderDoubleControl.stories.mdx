import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { SliderDoubleControl } from '../SliderDoubleControl';
import { mockPriceFilter } from '../mocks.tsx';
import { SearchDecorator } from '../../../.storybook/decorators/SearchDecorator';

<Meta
  title="PLP/PriceFacetSliderDoubleControl"
  component={SliderDoubleControl}
  parameters={{
    layout: 'centered',
  }}
  decorators={[SearchDecorator]}

/>

export const Template = (args) => <SliderDoubleControl {...args} />;

# Slider

```
import { SliderDoubleControl } from '../SliderDoubleControl'
```

<Canvas>
  <Story
    name="PriceFacetSliderDoubleControl"
    args={{
      filterData: mockPriceFilter,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
