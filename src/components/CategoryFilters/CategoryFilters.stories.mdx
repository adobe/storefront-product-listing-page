import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { CategoryFilters } from '../../../components';
import { mockPriceFilter } from '../mocks.tsx';

<Meta
  title="PLP widget/CategoryFilters"
  component={CategoryFilters}
  parameters={{
    layout: 'centered', // centered | fullscreen
  }}
  decorators={[]}
/>

export const Template = (args) => <CategoryFilters {...args} />;

# CategoryFilters

WIP

```
import { CategoryFilters } from './Facets'
```

<Canvas>
  <Story
    name="CategoryFilters"
    args={{
      filterData: mockPriceFilter,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
