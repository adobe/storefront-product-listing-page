import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { CategoryFilters } from './CategoryFilters';
import { mockFilters } from '../mocks.tsx';
import { action } from '@storybook/addon-actions';

<Meta
  title="PLP/CategoryFilters"
  component={CategoryFilters}
  parameters={{
    layout: 'centered',
  }}
  decorators={[]}
/>

export const Template = (args) => <CategoryFilters {...args} />;

# CategoryFilters

Sidebar of the PLP containing product filters

```
import { CategoryFilters } from './CategoryFilters'
```

<Canvas>
  <Story
    name="CategoryFilters"
    args={{
      loading: false,
      facets: mockFilters,
      totalCount: 15,
      categoryName: 'Pants',
      phrase: 'pants',
      showFilters: true,
      setShowFilters: action('setShowFilters'),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
