import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { InputButtonGroup } from '../../components/InputButtonGroup'
import { TextWithTooltip } from "./TextWithTooltip";

<Meta
    title="Examples: Slots/InputButtonGroup"
    component={InputButtonGroup}
    parameters={{
        layout: "centered"
    }}
/>

export const Template = args => <InputButtonGroup {...args} />;

# Filter

<Canvas>
    <Story
        name="InputButtonGroupSlotOverride"
        args={{
            title: "Milo",
            attribute: "milo",
            buckets: [
                { title: "Beta Hydri" },
                { title: "Omicron Eridani" },
                { title: "Alpha Centauri Proxima Centauri" },
            ],
            isSelected: () => Math.floor(Math.random() * 10) % 2 === 0,
            onChange: () => {},
            type: "checkbox",
            inputGroupTitleSlot: (title) => {
                return <TextWithTooltip text={title} tooltipText="galaxy stuff"/>
            }
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
