import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
// import { action } from '@storybook/addon-actions';
import { FilterButton } from "@adobe/ds-widget-sdk/ui-kit";

<Meta
    title="Core - UIKit/FilterButton"
    component={FilterButton}
    parameters={{
        layout: "centered", // centered | fullscreen
    }}
/>

export const Template = args => <FilterButton {...args} />;

# FilterButton

WIP

```
import { FilterButton } from '@adobe/ds-widget-sdk/ui-kit'
```

<Canvas>
    <Story
        name="FilterButton"
        args={{
            onClick: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
