import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
// import { action } from '@storybook/addon-actions';
import { SortDropdown } from "@adobe/ds-widget-sdk/src/ui-kit";

<Meta
    title="Core - UIKit/SortDropdown"
    component={SortDropdown}
    parameters={{
        layout: "centered", // centered | fullscreen
    }}
/>

export const Template = args => <SortDropdown {...args} />;

# ProductItem

WIP

```
import { SortDropdown } from '@adobe/ds-widget-sdk/src/ui-kit'
```

<Canvas>
    <Story
        name="SortDropdown"
        args={{
            value: "Most relevant",
            sortOptions: [
                { label: "Most relevant", value: "relevance_DESC" },
                { label: "Price: Low to High", value: "price_ASC" },
                { label: "Price: High to Low", value: "price_DESC" },
            ],
            onChange: val => {
                console.log(val);
            },
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
