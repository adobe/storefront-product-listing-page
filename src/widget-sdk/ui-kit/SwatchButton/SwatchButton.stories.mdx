import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { SwatchButton } from "../SwatchButton";

<Meta
    title="UIKit/SwatchButton"
    component={SwatchButton}
    parameters={{
        layout: "centered",
    }}
/>

export const Template = args => <SwatchButton {...args} />;

# SwatchButton

WIP

```
import { SwatchButton } from '../SwatchButton'
```

<Canvas>
    <Story
        name="Text - Unchecked"
        args={{
            value: "32",
            type: "text",
            checked: false,
            onClick: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Text - Checked"
        args={{
            value: "32",
            type: "text",
            checked: true,
            onClick: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Color - Unchecked"
        args={{
            value: "blue",
            type: "color",
            checked: false,
            onClick: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Color - Checked"
        args={{
            value: "blue",
            type: "color",
            checked: true,
            onClick: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
