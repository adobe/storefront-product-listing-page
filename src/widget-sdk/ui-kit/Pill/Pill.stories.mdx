import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { Pill } from "../Pill";

<Meta
    title="UIKit/Pill"
    component={Pill}
    parameters={{
        layout: "centered"
    }}
/>

export const Template = args => <Pill {...args} />;

# Pill



```
import { Pill } from '../Pill'
```

<Canvas>
    <Story
        name="Pill"
        args={{
            onClick: () => console.log("Clicked one me!"),
            label: "Pink",
            classes: "",

        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
