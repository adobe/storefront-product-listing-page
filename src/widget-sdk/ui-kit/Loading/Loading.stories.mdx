import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
// import { action } from '@storybook/addon-actions';
import { Loading } from "@adobe/ds-widget-sdk/ui-kit";

<Meta
    title="Core - UIKit/Loading"
    component={Loading}
    parameters={{
        layout: "centered", // centered | fullscreen
    }}
/>

export const Template = args => <Loading {...args} />;

# Loading

WIP

```
import { Loading } from '@adobe/ds-widget-sdk/ui-kit'
```

<Canvas>
    <Story
        name="Loading"
        args={{
            label: "Loading...",
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
