import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
// import { action } from '@storybook/addon-actions';
import { NoResults } from "@adobe/ds-widget-sdk/ui-kit";

<Meta
    title="Core - UIKit/NoResults"
    component={NoResults}
    parameters={{
        layout: "centered", // centered | fullscreen
    }}
/>

export const Template = args => <NoResults {...args} />;

# NoResults

WIP

```
import { NoResults } from '@adobe/ds-widget-sdk/ui-kit'
```

<Canvas>
    <Story
        name="NoResults"
        args={{
            heading: "No results for your search.",
            subheading: "Please try again...",
            isError: false,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
