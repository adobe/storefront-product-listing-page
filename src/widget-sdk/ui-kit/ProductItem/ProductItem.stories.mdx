import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { sampleProductDiscounted, sampleProductNotDiscounted, sampleProductNoImage } from "./MockData";
import { ProductItem } from "../ProductItem";

<Meta
    title="UIKit/ProductItem"
    component={ProductItem}
    parameters={{
        layout: "centered"
    }}
/>

export const Template = args => <ProductItem {...args} />;

# ProductItem

WIP

```
import { ProductItem } from '../ProductItem'
```

<Canvas>
    <Story name="ProductItem" args={{ item: sampleProductNotDiscounted }}>
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="ProductItemWithDiscount" args={{ item: sampleProductDiscounted }}>
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="ProductItemNoImage" args={{ item: sampleProductNoImage }}>
        {Template.bind({})}
    </Story>
</Canvas>
