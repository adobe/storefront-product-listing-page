import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
// import { action } from '@storybook/addon-actions';
import { ProductList } from "@adobe/ds-widget-sdk/src/ui-kit";
import { products } from "./MockData";

<Meta
    title="Core - UIKit/ProductList"
    component={ProductList}
    parameters={{
        layout: "centered", // centered | fullscreen
    }}
/>

export const Template = args => <ProductList {...args} />;

# ProductList

WIP

```
import { ProductList } from '@adobe/ds-widget-sdk/src/ui-kit'
```

<Canvas>
    <Story name="ProductList" args={{ products, numberOfColumns: 4 }}>
        {Template.bind({})}
    </Story>
</Canvas>
