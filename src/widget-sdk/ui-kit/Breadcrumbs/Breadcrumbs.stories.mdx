import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
// import { action } from '@storybook/addon-actions';
import { Breadcrumbs } from "@adobe/ds-widget-sdk/ui-kit";
import { pages } from "./MockPages.ts";

<Meta
    title="Core - UIKit/Breadcrumbs"
    component={Breadcrumbs}
    parameters={{
        layout: "centered", // centered | fullscreen
    }}
/>

export const Template = args => <Breadcrumbs {...args} />;

# Breadcrumbs

WIP

```
import { Breadcrumbs } from '@adobe/ds-widget-sdk/ui-kit'
```

<Canvas>
    <Story name="Breadcrumbs" args={{ pages }}>
        {Template.bind({})}
    </Story>
</Canvas>
